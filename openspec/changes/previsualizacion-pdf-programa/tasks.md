# Tasks: Previsualización de Programa a PDF

- [x] 1. Añadir señal y métodos de carga de datos para semanas en la vista previa (`previewWeeksDataMap`, `fetchPreviewDataForSelectedWeeks`).
- [x] 2. Rediseñar el template del modal de exportación en `asignacion-list.component.ts` con vista dividida (sidebar + hoja de previsualización en vivo).
- [x] 3. Agregar estilos SCSS para el diseño de la hoja imprimible (.pdf-preview-sheet) y el contenedor de previsualización.
- [x] 4. Agregar reglas CSS `@media print` para asegurar una impresión/generación de PDF limpia y formateada sin elementos de la UI web.
- [x] 5. Probar compilación con `npx ng build --configuration development` y verificar visualmente la previsualización.
