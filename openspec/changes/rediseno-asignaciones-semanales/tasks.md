# Tasks: Rediseño de Asignaciones Unificadas por Semana

- [x] 1. Actualizar el componente `asignacion-list.component.ts` para simplificar el formulario y la grilla a una asignación única semanal por función.
- [x] 2. Ajustar la vista principal de `/asignaciones` y el modal de edición para reflejar la columna unificada "Asignación".
- [x] 3. Rediseñar la plantilla de vista previa del PDF imprimible (`.pdf-printable-sheet`) con el formato exacto de 2 columnas (Función | Asignación) de la imagen de referencia.
- [x] 4. Validar compilación con `npx ng build --configuration development` e impresión A4 de 5 semanas en 1 sola hoja.
