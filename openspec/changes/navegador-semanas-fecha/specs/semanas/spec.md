# Feature Spec: Navegador de Semanas

## Requirements
1. The system shall render previous/next week navigation buttons (`<` and `>`).
2. The system shall render a datepicker control to select any date.
3. When any date is selected, the system shall calculate the Monday of that week.
4. The system shall auto-select or auto-create the corresponding week.
