# Tasks Checklist

- [x] 1. Agregar interfaz `CreateUserRequest` y método `createUser` en `user.service.ts`.
- [x] 2. Agregar botón "Nuevo Usuario" y modal de creación en `usuarios-list.component.ts`.
- [x] 3. Agregar estilos CSS para el modal y botón en `usuarios-list.component.ts`.
- [x] 4. Probar la creación de usuarios desde la UI.
