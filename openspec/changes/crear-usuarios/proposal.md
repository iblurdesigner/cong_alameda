# Proposal: Creación de Usuarios

## Intent
Permitir crear nuevos usuarios directamente desde la interfaz de administración en `/usuarios`.

## Scope
- Frontend: `user.service.ts` y `usuarios-list.component.ts`.
- Backend: Endpoint `POST /api/users` existente.
