# OpenSpec Configuration — cong_alameda
# Auto-generated by sdd-init (hybrid mode)
# Do not edit manually unless you understand the consequences.

context:
  project: cong_alameda
  description: Gestión para el Cuerpo de Ancianos — casas, visitas, grupos, territorios, asignaciones
  phases:
    - fase-1: Superintendente Servicio (casas, visitas, notificaciones)
    - fase-2: Visita Superintendente (grupos, territorios, semanas)
    - fase-3: Asignaciones Internas

stack:
  backend:
    language: Go 1.21
    framework: Fiber v2.52
    database: PostgreSQL 15 (pgx/v5)
    auth: JWT v5 (golang-jwt)
    arch: handlers → services → repositories
  frontend:
    language: TypeScript 5.9
    framework: Angular 21 (standalone, signals)
    styling: SCSS
    arch: Container/Presentational (core/features/shared)
  devops:
    docker: multi-stage builds
    compose: v3.8
    proxy: nginx alpine

strict_tdd: true
# strict_tdd resolved from: test runner detected (go test + Jest), no explicit false marker

testing:
  backend:
    runner: go test (stdlib)
    unit: true
    integration: true
    e2e: false
    coverage: go test -cover
    linter: none detected
    formatter: none detected
  frontend:
    runner: Jest 30
    unit: true
    integration: false
    e2e: false
    coverage: jest --coverage (text, lcov, html)
    linter: none detected
    type_checker: TypeScript 5.9 (strict mode)
    formatter: none detected

sdd_phases:
  sdd-propose:
    required: true
    skip_if_trivial: false
  sdd-spec:
    required: true
    skip_if_trivial: false
  sdd-design:
    required: true
    skip_if_trivial: false
  sdd-tasks:
    required: true
    skip_if_trivial: false
  sdd-apply:
    required: true
  sdd-verify:
    required: true
  sdd-archive:
    required: true

discrepancies:
  - file: AGENTS.md
    field: Backend framework
    states: Gin
    actual: Fiber v2.52
    sev: medium
  - file: AGENTS.md
    field: Frontend version
    states: Angular 17+
    actual: Angular 21
    sev: low
